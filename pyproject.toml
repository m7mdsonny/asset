[project]
name = "gacms"
version = "1.0.0"
description = "Group Asset Custody Management System"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
